# Simulation parameters
data:
  n_trials: 32
  batch_size: 16

# Model parameters
model:
  n_components: 5
  kernel_size: 30
  n_channels: 1
  lambda: 0.01
  n_iterations: 25
  epochs: 20
  window: true
  list_d: true

# Training options
training:
  n_runs: 20